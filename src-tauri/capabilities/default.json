{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for smol_md.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "file-commands"
  ]
}
